classes:
- system.linux.system.repo.mcp.extra
- cluster.mcp11ovs.infra
parameters:
  linux:
    network:
      interface:
        eth0: ${_param:linux_dhcp_interface}
        eth1: ${_param:linux_single_interface}
